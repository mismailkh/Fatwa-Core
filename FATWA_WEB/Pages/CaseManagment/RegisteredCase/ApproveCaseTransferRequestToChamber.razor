@page "/CaseTransferRequest-ToChamber/{ChamberId:int}"

<div class="row">
    <div class="col-lg-12">
        <div class="form-group rz-calendar-webkit-fill mb-0">
            <RadzenLabel Text="@(translationState.Translate("Chamber") + " *")" Style="display:block;" />
            <RadzenDropDown Data="@(allChambers)" AllowFiltering="true" FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" AllowClear="true"
                            TextProperty="@(Thread.CurrentThread.CurrentUICulture.Name == "en-US" ? "Name_En" : "Name_Ar")"
                            Name="ChamberName" ValueProperty="Id" Placeholder="@translationState.Translate("Select")" Change="@OnChangeChamber"
                            Style="display:block;" @bind-Value="ChamberId">
            </RadzenDropDown>
        </div>
        <div class="form-group rz-calendar-webkit-fill mb-0">
            <RadzenLabel Text="@(translationState.Translate("Chamber_Number") + " *")" Style="display:block;" />
            <RadzenDropDown Data="@(ChamberNumbers)" AllowFiltering="true" FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" AllowClear="true"
                            TextProperty="Number" ValueProperty="Id" Placeholder="@translationState.Translate("Select")"
                            Style="display:block;" @bind-Value="ChamberNumberId">
            </RadzenDropDown>
        </div>
    </div>
</div>
<div class="row mt-4 popup-buttons-alignment">
    <div class="col-md-12">
        <RadzenButton Click="@(()=>UpdateRegisteredCaseChamberNumber())" Text="@translationState.Translate("Submit")" Style="width: 120px" Disabled="@(ChamberId == 0||ChamberNumberId == 0)" />
        <RadzenButton Click="@((args) => dialogService.Close())" ButtonStyle="ButtonStyle.Secondary" Text="@translationState.Translate("Cancel")" Style="width: 120px" Class="mr-1" />
    </div>
</div>
